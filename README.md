# ZK50-ODOO — ระบบซิงค์เวลาทำงานอัตโนมัติ

สคริปต์นี้ใช้สำหรับซิงค์บันทึกการลงเวลาจากเครื่องสแกนนิ้ว **ZKTeco** (เช่น K50) เข้าสู่ระบบ **Odoo** แบบอัตโนมัติผ่าน **XML-RPC API**

## 📋 สารบัญ
- [ภาพรวมระบบ](#ภาพรวมระบบ)
- [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
- [การติดตั้งและตั้งค่า](#การติดตั้งและตั้งค่า)
- [การใช้งาน](#การใช้งาน)
- [การทำงานของโปรแกรม](#การทำงานของโปรแกรม)
- [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🎯 ภาพรวมระบบ

โปรแกรมนี้เชื่อมต่อระหว่าง 2 ระบบ:

1. **ZKTeco K50** - เครื่องสแกนนิ้วบันทึกเวลาเข้า-ออกงาน
2. **Odoo ERP** - ระบบบริหารจัดการทรัพยากรบุคคล (HR Module)

**วัตถุประสงค์:** ทำการซิงค์ข้อมูลการลงเวลาจากเครื่องสแกนไปยัง Odoo อัตโนมัติ เพื่อลดการป้อนข้อมูลซ้ำและลดข้อผิดพลาด

---

## 🔧 เทคโนโลยีที่ใช้

### XML-RPC คืออะไร?

**XML-RPC** (XML Remote Procedure Call) คือโพรโทคอลสำหรับเรียกใช้ฟังก์ชันระหว่างเครื่องผ่านเครือข่าย

**การทำงาน:**
```
Client (Python)  →  HTTP Request (XML)  →  Server (Odoo)
                                        
execute_kw()     →  <methodCall>        →  Odoo ประมวลผล
                    <params>...</params>
                    
Client ←  HTTP Response (XML)  ←  Return ผลลัพธ์
```

### ทำไมต้องใช้ XML-RPC?

✅ **รองรับหลายภาษา** - ใช้ได้กับ Python, PHP, Java, C#, etc.  
✅ **เป็นมาตรฐาน** - Odoo รองรับ XML-RPC โดยตรง  
✅ **ไม่ต้องติดตั้ง Library พิเศษ** - Python มี `xmlrpc.client` ในตัว  
✅ **ใช้งานง่าย** - ไม่ต้องติดตั้ง module เพิ่มใน Odoo  

### execute_kw คืออะไร?

`execute_kw` เป็นฟังก์ชันหลักสำหรับเรียกใช้ทุกอย่างใน Odoo ผ่าน XML-RPC

**รูปแบบการใช้งาน:**
```python
models.execute_kw(
    database,    # ชื่อฐานข้อมูล
    uid,         # User ID
    password,    # API Key
    model,       # เช่น 'hr.employee', 'hr.attendance'
    method,      # เช่น 'search', 'read', 'create', 'write'
    args,        # Arguments
    kwargs       # Keyword Arguments
)
```

**ตัวอย่าง:**
```python
# ค้นหาพนักงาน
employee_ids = models.execute_kw(db, uid, pwd, 
    'hr.employee', 'search', 
    [[['barcode', '=', '123']]])

# สร้างบันทึกเวลา
models.execute_kw(db, uid, pwd, 
    'hr.attendance', 'create', 
    [{'employee_id': 5, 'check_in': '2026-01-14 09:00:00'}])
```

### Python Libraries ที่ใช้

- **`pyzk`** - เชื่อมต่อกับเครื่อง ZKTeco
- **`xmlrpc.client`** - เชื่อมต่อกับ Odoo ผ่าน XML-RPC
- **`python-dotenv`** - จัดการ environment variables

---

## 🚀 การติดตั้งและตั้งค่า

### สิ่งที่ต้องเตรียม
- ✅ Python 3.8 ขึ้นไป
- ✅ คอมพิวเตอร์เชื่อมต่อเครือข่ายเดียวกับเครื่องสแกน ZKTeco
- ✅ สิทธิ์เข้าถึง Odoo API (API Key หรือ Password)

## ตั้งค่าไฟล์ `.env`
สร้างไฟล์ `.env` ในโฟลเดอร์โปรเจค และใส่ค่าดังตัวอย่าง:

```
ODOO_URL=https://your-odoo.example.com
ODOO_DB=your_db
ODOO_USER=you@example.com
ODOO_API_KEY=your_api_key_or_password
ZK_DEVICE_IP=192.168.1.130
ZK_DEVICE_PORT=4370
```

หมายเหตุ: ค่าตัวอย่างด้านบนต้องแก้ให้ตรงกับการตั้งค่าจริงของระบบคุณ

## ติดตั้ง dependencies
เปิด PowerShell ในโฟลเดอร์โปรเจค แล้วรันคำสั่ง:

```powershell
python -m pip install --upgrade pip
python -m pip install python-dotenv pyzk
```

## รันสคริปต์แบบทดสอบ (ครั้งเดียว)
รันคำสั่งนี้เพื่อลองดึงบันทึกล่าสุดจากเครื่องสแกนและส่งไป Odoo:

```powershell
python app_secure.py
```

ผลลัพธ์จะพิมพ์ข้อความสรุปว่ามีการสร้างเรคอร์ด `hr.attendance` สำหรับพนักงานหมายเลขใดและเวลาใด

---

## 📖 การทำงานของโปรแกรม

### Flow การทำงานหลัก

```
1. เชื่อมต่อ ZKTeco
   ↓
2. ดึงข้อมูลการลงเวลาทั้งหมด
   ↓
3. เชื่อมต่อ Odoo ผ่าน XML-RPC
   ↓
4. สำหรับแต่ละรายการ:
   ├─→ ค้นหาพนักงานใน Odoo (จาก barcode)
   │   ├─→ พบพนักงาน?
   │   │   ├─→ YES: ปรับเวลา (UTC+7)
   │   │   │   ├─→ มี check_in ค้างอยู่?
   │   │   │   │   ├─→ YES: อัปเดต check_out
   │   │   │   │   └─→ NO: สร้าง check_in ใหม่
   │   │   └─→ NO: แสดง warning
   │   └─→ ตรวจสอบข้อมูลซ้ำ (ป้องกัน duplicate)
   └─→ รายการถัดไป
```

### กลไกสำคัญ 3 อย่าง

#### 1️⃣ Retry Mechanism (ป้องกันข้อผิดพลาดชั่วคราว)

```python
def exec_with_retry(models, db, uid, pwd, model, method, 
                    args=None, kwargs=None, retries=3, delay=1):
```

**การทำงาน:**
- พยายามเรียก API สูงสุด **3 ครั้ง**
- แต่ละครั้งที่ล้มเหลว จะหน่วงเวลา `delay × attempt` วินาที
- บันทึก log เพื่อ debug

**ประโยชน์:**
- ✅ แก้ปัญหา network timeout ชั่วคราว
- ✅ จัดการกับ server ที่โหลดสูง
- ✅ เพิ่มความน่าเชื่อถือของระบบ

#### 2️⃣ Timezone Adjustment (ปรับเวลาให้ตรงกับเวลาไทย)

```python
adjusted_ts = att.timestamp - timedelta(hours=7)
check_time = adjusted_ts.strftime('%Y-%m-%d %H:%M:%S')
```

**เหตุผล:**
- เครื่อง ZKTeco บันทึกเวลาเป็น UTC (หรือ local time)
- Odoo ใช้ UTC เป็นมาตรฐาน
- ต้องปรับลบ 7 ชั่วโมงสำหรับเวลาไทย (UTC+7)

#### 3️⃣ Duplicate Prevention (ป้องกันข้อมูลซ้ำ)

```python
# ตรวจสอบว่ามีข้อมูลนี้อยู่แล้วหรือไม่
existing = exec_with_retry(models, ODOO_DB, uid, ODOO_PASS, 
    'hr.attendance', 'search', 
    args=[[['employee_id', '=', emp_id], 
           ['check_in', '=', check_time]]])

if not existing:
    # สร้างใหม่เมื่อยังไม่มีข้อมูล
    exec_with_retry(models, ODOO_DB, uid, ODOO_PASS, 
        'hr.attendance', 'create', ...)
```

### ตรรกะการจัดการ Check-In / Check-Out

```
สถานการณ์ที่ 1: ไม่มี check_in ค้าง
├─→ ตรวจสอบว่ามีข้อมูลนี้อยู่แล้วหรือไม่
└─→ สร้าง check_in ใหม่ (ถ้ายังไม่มี)

สถานการณ์ที่ 2: มี check_in ค้างอยู่ (ยังไม่ check_out)
├─→ อ่าน check_in time
├─→ ตรวจสอบเวลา:
│   ├─→ ถ้าเวลาสแกน < check_in (ผิดปกติ)
│   │   └─→ ใช้ check_in + 1 วินาที เป็น check_out
│   └─→ ถ้าเวลาสแกน >= check_in (ปกติ)
│       └─→ ใช้เวลาสแกนเป็น check_out
└─→ อัปเดต check_out พร้อม error handling
```

### Fallback Strategy (กรณีเกิดข้อผิดพลาด)

```python
try:
    # พยายามอัปเดต check_out ตามปกติ
    exec_with_retry(models, ..., 'write', 
        args=[[oid], {'check_out': check_time}])
except xmlrpc.client.Fault as f:
    # ถ้า error (เช่น constraint violation)
    # ใช้ fallback: check_in + 1 วินาที
    fallback_dt = check_in_dt + timedelta(seconds=1)
    exec_with_retry(models, ..., 'write', 
        args=[[oid], {'check_out': fallback_str}])
```

**เหตุผล:** Odoo มี constraint ที่ check_out ต้องมากกว่า check_in เสมอ

---

## 💻 การใช้งาน

---

## 🚀 ฟีเจอร์อัตโนมัติ (ใหม่!)

ระบบได้รับการอัปเกรดให้ทำงานอัตโนมัติเต็มรูปแบบ:

### 1. Auto Check-In (10:00 น.)
- **ปัญหา:** พนักงานลืมสแกนเข้างานตอนเช้า
- **การทำงาน:** ระบบตรวจสอบพนักงานที่มี Barcode แต่ยังไม่มี Attendance ในวันนั้น และสร้าง Check-In เวลา **10:00:00** ให้อัตโนมัติ

### 2. Auto Check-Out (เที่ยงคืน)
- **ปัญหา:** พนักงานลืมสแกนออกงานตอนเย็น ทำให้วันถัดไปสแกนเข้าไม่ได้
- **การทำงาน:** ระบบตรวจสอบ Attendance ที่ยังเปิดค้างอยู่ (ไม่มี Check-Out) ของวันก่อนหน้า และปิดงานด้วยเวลา **23:59:59** ให้อัตโนมัติ

### 3. Fully Automated (ไม่ต้องรันเอง)
- ระบบทำงานผ่าน **Windows Task Scheduler**
- รันอัตโนมัติตามเวลาที่กำหนด (ไม่ต้องเปิดโปรแกรมทิ้งไว้)

---

## ⚙️ การตั้งค่าระบบอัตโนมัติ

### 1. ลงทะเบียนงานอัตโนมัติ (ครั้งเดียวจบ)

เปิด **PowerShell** (Run as Administrator) แล้วรันคำสั่ง:

```powershell
powershell -ExecutionPolicy Bypass -File .\register_all_tasks.ps1
```

ระบบจะสร้าง 3 งานให้อัตโนมัติ:
1. **ZK50-ODOO Sync** (ทุก 5 นาที) - ซิงค์ข้อมูลปกติ
2. **ZK50-ODOO Auto Check-In** (10:00 น.) - แก้ปัญหาลืมสแกนเข้า
3. **ZK50-ODOO Auto Check-Out** (00:00 น.) - แก้ปัญหาลืมสแกนออก

### 2. ยกเลิกงานอัตโนมัติ (ถ้าต้องการหยุด)

```powershell
powershell -ExecutionPolicy Bypass -File .\unregister_tasks.ps1
```

### 3. ปรับตั้งเวลา (Optional)

คุณสามารถเปลี่ยนเวลา Auto Check-In/Out ได้ในไฟล์ `.env`:

```ini
# เวลาสำหรับ Auto Check-In (Default: 10:00)
AUTO_CHECKIN_HOUR=10
AUTO_CHECKIN_MINUTE=0

# เวลาสำหรับ Auto Check-Out (Default: 23:59)
AUTO_CHECKOUT_HOUR=23
AUTO_CHECKOUT_MINUTE=59
```

---

## 📖 คู่มือการใช้งาน (Manual Mode)

หากต้องการรันคำสั่งด้วยตนเองผ่าน Command Line:

| คำสั่ง | รายละเอียด |
|--------|------------|
| `python app_secure.py` | ซิงค์ข้อมูลจากเครื่องสแกน (ปกติ) |
| `python app_secure.py --auto-checkout` | รัน Auto Check-Out (สำหรับเมื่อวาน) |
| `python app_secure.py --auto-checkin` | รัน Auto Check-In (สำหรับวันนี้) |
| `python app_secure.py --help` | ดูวิธีใช้งานทั้งหมด |

---

## � โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|------|---------|
| `app_secure.py` | **Core Program:** รวม logic ทั้งหมดในไฟล์เดียว |
| `.env` | **Config:** เก็บค่า IP, Password, และเวลาตั้งค่า |
| `register_all_tasks.ps1` | **Setup:** ลงทะเบียน Task Scheduler ทั้งหมด |
| `unregister_tasks.ps1` | **Cleanup:** ลบ Task Scheduler ทั้งหมด |
| `run_sync.bat` | **Wrapper:** Batch file สำหรับรันคำสั่งต่าง ๆ |
| `sync_output.log` | **Log:** บันทึกประวัติการทำงานและข้อผิดพลาด |
| `test_connection.py` | **Utility:** ทดสอบการเชื่อมต่อ ZKTeco |

---

## 📊 สรุปสถานะล่าสุด (14 ม.ค. 2026)

✅ **Connectivity:** เชื่อมต่อ ZKTeco และ Odoo ได้สมบูรณ์
✅ **Sync Logic:** Retry 3 ครั้ง + ป้องกันข้อมูลซ้ำ + Fallback Strategy
✅ **Auto Features:** รองรับ Auto Check-In และ Auto Check-Out
✅ **Automation:** ตั้งเวลาผ่าน Task Scheduler เรียบร้อย

**พร้อมใช้งานทันที!** 🚀

---

## 🙋 การแก้ไขปัญหาเบื้องต้น

1. **เชื่อมต่อไม่ได้?** → รัน `python test_connection.py`
2. **ไม่ซิงค์อัตโนมัติ?** → เช็ค Task Scheduler หรือดูไฟล์ `sync_output.log`
3. **เวลาไม่ตรง?** → ตรวจสอบ Timezone ใน Odoo และเครื่องสแกน

---


